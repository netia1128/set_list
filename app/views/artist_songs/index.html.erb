<h1>Songs</h1>

<% @songs.each do |song| %>
  <h3><%= link_to song.title, "/songs/#{song.id}" %></h3>
  <p>Play Count: <%= song.play_count %></p>
  <p>Length: <%= song.length %></p>
<% end %>

<p>Average Song Length for <%= @artist.name %> <%= @artist.average_song_length %></p>